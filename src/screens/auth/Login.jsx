// S-01 · Login (UID + Password)
// Owner: Ellie · Traces to REQ-AUTH-001
// TODO (Ellie): replace <PlaceholderScreen/> with the real screen. Build it with
// components from '../../components/ui'; copy src/screens/athlete/Dashboard.jsx
// (A-01) as a starting point. Then set status: 'done' in src/data/screens.js.
import PlaceholderScreen from '../../components/PlaceholderScreen'

export default function Login() {
  return <PlaceholderScreen id="S-01" />
}
