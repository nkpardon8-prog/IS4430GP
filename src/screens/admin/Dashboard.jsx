// D-01 · Admin Dashboard / Settings
// Owner: Patrick · Traces to REQ-ROLE-004 §3.4
// TODO (Patrick): replace <PlaceholderScreen/> with the real screen. Build it with
// components from '../../components/ui'; copy src/screens/athlete/Dashboard.jsx
// (A-01) as a starting point. Then set status: 'done' in src/data/screens.js.
import PlaceholderScreen from '../../components/PlaceholderScreen'

export default function AdminDashboard() {
  return <PlaceholderScreen id="D-01" />
}
